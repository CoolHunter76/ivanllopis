# Landing V2 hobbies compatibility fix

This package replaces `templates/hobbies.html`, updates the eight translation JSON files with the flat keys required by Landing V2, and includes a CSS addition for `static/css/site.css`.

Append `site-css-hobbies-addition.css` to `static/css/site.css`, then delete the auxiliary CSS patch file.
