(() => {"use strict";const root=document.querySelector("[data-project-world]"),canvas=document.querySelector("[data-world-canvas]");if(!root)return;const details={edge:"EDGE // Browser request over HTTPS. Public traffic reaches the reverse proxy.",nginx:"NGINX // TLS termination and reverse proxy. Production and staging remain isolated.",docker:"DOCKER // Reproducible application runtime and deployment boundary.",fastapi:"FASTAPI // Python application layer: routing, templates, validation, integrations and APIs.",services:"SERVICES // GitHub data, Resend email, SEO middleware and application services."},detail=document.querySelector("[data-node-detail]");document.querySelectorAll("[data-node]").forEach(button=>button.addEventListener("click",()=>{if(detail)detail.textContent=details[button.dataset.node]||"";}));if(!canvas||matchMedia("(prefers-reduced-motion: reduce)").matches)return;const ctx=canvas.getContext("2d");let points=[];const resize=()=>{const r=canvas.getBoundingClientRect(),ratio=Math.min(devicePixelRatio||1,1.5);canvas.width=r.width*ratio;canvas.height=r.height*ratio;ctx.setTransform(ratio,0,0,ratio,0,0);points=Array.from({length:Math.min(85,Math.max(35,Math.floor(r.width/15)))},()=>({x:Math.random()*r.width,y:Math.random()*r.height,vx:(Math.random()-.5)*.16,vy:(Math.random()-.5)*.16}));};const draw=()=>{const r=canvas.getBoundingClientRect();ctx.clearRect(0,0,r.width,r.height);ctx.fillStyle="#39ff88";ctx.strokeStyle="rgba(57,255,136,.11)";points.forEach((p,i)=>{p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>r.width)p.vx*=-1;if(p.y<0||p.y>r.height)p.vy*=-1;ctx.globalAlpha=.5;ctx.fillRect(p.x,p.y,1.5,1.5);for(let j=i+1;j<points.length;j++){const q=points[j],dx=p.x-q.x,dy=p.y-q.y,d=Math.hypot(dx,dy);if(d<105){ctx.globalAlpha=(1-d/105)*.45;ctx.beginPath();ctx.moveTo(p.x,p.y);ctx.lineTo(q.x,q.y);ctx.stroke();}}});ctx.globalAlpha=1;requestAnimationFrame(draw);};addEventListener("resize",resize,{passive:true});resize();requestAnimationFrame(draw);})();
