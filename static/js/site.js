(() => {"use strict";const navButton=document.querySelector(".nav-toggle"),nav=document.querySelector("#site-nav"),switcher=document.querySelector("[data-language-switcher]"),current=switcher?.querySelector(".language-current"),menu=switcher?.querySelector(".language-menu");if(navButton&&nav){navButton.addEventListener("click",()=>{const open=nav.classList.toggle("open");navButton.setAttribute("aria-expanded",String(open));});nav.querySelectorAll("a").forEach(link=>link.addEventListener("click",()=>{nav.classList.remove("open");navButton.setAttribute("aria-expanded","false");}));}if(current&&menu){current.addEventListener("click",()=>{const open=menu.hidden;menu.hidden=!open;current.setAttribute("aria-expanded",String(open));});document.addEventListener("click",event=>{if(!switcher.contains(event.target)){menu.hidden=true;current.setAttribute("aria-expanded","false");}});menu.querySelectorAll("a").forEach(link=>link.addEventListener("click",event=>{event.preventDefault();const parts=location.pathname.split("/").filter(Boolean);parts[0]=link.dataset.language;location.assign(`/${parts.join("/")}${location.hash}`);}));}const reveal=()=>{if(!("IntersectionObserver" in window)){document.querySelectorAll(".reveal").forEach(el=>el.classList.add("visible"));return;}const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("visible");observer.unobserve(entry.target);}}),{threshold:.1});document.querySelectorAll(".reveal").forEach(el=>observer.observe(el));};reveal();})();