(() => {"use strict";const navButton=document.querySelector(".nav-toggle");const nav=document.querySelector("#site-nav");const language=document.querySelector("#language-select");if(navButton&&nav){navButton.addEventListener("click",()=>{const open=nav.classList.toggle("open");navButton.setAttribute("aria-expanded",String(open));});nav.querySelectorAll("a").forEach(link=>link.addEventListener("click",()=>{nav.classList.remove("open");navButton.setAttribute("aria-expanded","false");}));}if(language){language.addEventListener("change",()=>{const path=window.location.pathname.split("/").filter(Boolean);path[0]=language.value;window.location.assign(`/${path.join("/")}${window.location.hash}`);});}const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("visible");observer.unobserve(entry.target);}}),{threshold:.12});document.querySelectorAll(".reveal").forEach(section=>observer.observe(section));})();
