(() => {"use strict";const root=document.querySelector("[data-hobbies-portal]");if(!root)return;const reduced=matchMedia("(prefers-reduced-motion: reduce)").matches,coarse=matchMedia("(pointer: coarse)").matches;root.querySelectorAll("[data-hobby-card]").forEach(card=>{if(!reduced&&!coarse){card.addEventListener("pointermove",event=>{const r=card.getBoundingClientRect(),x=(event.clientX-r.left)/r.width-.5,y=(event.clientY-r.top)/r.height-.5;card.style.transform=`perspective(1000px) rotateX(${-y*2.8}deg) rotateY(${x*3.5}deg) translateY(-7px)`});card.addEventListener("pointerleave",()=>{card.style.transform=""})}if(card.classList.contains("is-live")){card.addEventListener("click",event=>{if(reduced||event.defaultPrevented||event.button!==0||event.ctrlKey||event.metaKey||event.shiftKey||event.altKey)return;event.preventDefault();card.classList.add("is-portal-entering");window.setTimeout(()=>location.assign(card.href),380)})}})})();
